#include "libft.h"
#include "memory.h"

t_garbage_col	*garbage_col(void);

void	test_garbage_col(void)
{
	// char	*ptr1;
	// char	*ptr2;
	//
	// ptr1 = alloc(1, 100);
	// ft_strlcpy(ptr1, "Hello ", 6);
	// ptr2 = alloc(1, 100);
	// ft_strlcpy(ptr2, "world", 6);
	//
	// printf("first alloc: %s\n", ptr1);
	// printf("second alloc: %s\n", ptr2);
	//
	// free_this(ptr2);
	// assure_memory();
}
